# GitHub Pages × Agent 技术博客：日常运营指南

> 适用场景：使用 `*.github.io` 仓库 + Jekyll（或同类静态站点）托管个人博客，并希望在写作与维护中**适度借助 Agent（IDE/Cursor、CLI、自动化脚本）**，同时保持内容可信度与职业形象。

---

## 一、仓库与发布链路（每周至少核对一次）

| 动作 | 说明 |
|------|------|
| **主干分支** | 博客常用 `main`；Pages 设置指向该分支根目录（或 `/docs`，若你改用 docs 构建）。 |
| **构建结果** | GitHub Pages 对 Jekyll 默认云端构建；推送后 1～5 分钟内生效，缓存可能导致稍晚可见。 |
| **构建失败邮件** | 在 GitHub 账号设置里打开 Actions/Pages 通知；`_config.yml` 语法错误、非法插件会导致构建失败。 |

**建议习惯**：每次推送到主干前，本地快速扫一眼 `_config.yml` 与新建文章的 YAML 头是否缩进正确（YAML 对空格敏感）。

---

## 二、固定目录约定（减少心智负担）

- **`_posts/`**：唯一正文文章目录；文件名必须带日期前缀，否则 permalink 可能异常。
- **`index.md`**：首页文案；`layout: home` 保留。
- **`about.md`**：个人介绍与联系方式入口。
- **`_config.yml`**：`title`、`url` 填你的站点根 URL；`username.github.io` 类型仓库的 `baseurl` 保持为空字符串。
- **不参与站点输出的文档**：可在 `_config.yml` 的 `exclude` 中列出（例如运营指南、私有草稿——草稿建议勿入库或以私密分支管理）。

---

## 三、发布一篇新文章的标准流程

1. **选题**：优先写「问题背景 → 方案对比 → 取舍 → 结果或教训」；避免纯摘要式罗列文档。
2. **新建文件**：`_posts/YYYY-MM-DD-slug.md`，slug 用英文小写与短横线。
3. **Front Matter**（必填字段示例）：

   ```yaml
   ---
   layout: post
   title: "中文标题写在这里"
   date: YYYY-MM-DD
   categories: [分类一]
   tags: [标签A, 标签B]
   ---
   ```

4. **本地预览**（可选）：`bundle exec jekyll serve`，检查代码块高亮、标题层级、链接。
5. **提交与推送**：

   ```text
   git add _posts/...
   git commit -m "post: 简短说明"
   git push origin main
   ```

6. **线上验收**：打开文章 URL，检查移动端排版与目录（长文建议手动加二级标题）。

---

## 四、用 Agent 辅助写作的边界（面试向博客尤其重要）

- **适合交给 Agent 的**：段落扩写草稿、把提纲变成连贯叙述、检查术语统一、生成 Mermaid 草图、把长回答改成更适合博客的结构。
- **必须自己把关的**：真实项目数据、公司合规立场、未公开的架构细节；**医疗/金融相关结论**避免让模型「编造法规条文」——法规请以官方文本为准。
- **降「AI 味」的实操**：写完后朗读一遍；删掉套话（「综上所述」「赋能」「在当今时代」）；用具体数字与错误案例代替空洞形容词；保留一点个人判断甚至吐槽（在职场上仍保持尊重与中性）。

---

## 五、SEO 与分享（性价比高的几项）

- **文章标题**：含清晰主题词（如 Agent、RAG、合规），但避免标题过长。
- **稳定 URL**：不要随意改旧文章文件名，会破坏外链。
- **站内交叉链接**：新文章引用旧文固定格式：`[标题](/year/month/day/slug/)`（以你站点实际 permalink 为准）。
- **可选**：在 `_config.yml` 已启用 `jekyll-seo-tag` / `jekyll-feed` 的前提下，把站点 URL 填对即可生成 meta 与 RSS。

---

## 六、安全与隐私（技术博客常见漏洞）

- **勿提交**：API Key、内网地址、客户名称与未公开数据；代码示例用占位符。
- **仓库可见性**：`github.io` 一般为公开仓库；若写敏感踩坑文，抽象化场景。
- **依赖**：`Gemfile` 使用 `github-pages` gem，由官方锁定版本，降低 Gem 供应链风险；勿随意添加未在 GitHub Pages 白名单的 Jekyll 插件。

---

## 七、频率与长线策略（面向求职）

- **节奏**：起步每月 1～2 篇深度文，比每周水文更有利于塑造「专业性」。
- **选题矩阵**：工程一篇（实现细节）、方法论一篇（评测/排障）、行业一篇（医疗或金融约束）。
- **与简历联动**：简历项目条目可在文末附「详见博客某篇」，面试官追问时有锚点。

---

## 八、故障速查

| 现象 | 可能原因 |
|------|----------|
| 推送后站点未更新 | Pages 构建延迟或未触发；查看 Actions / Pages 构建日志。 |
| 文章未出现 | 文件名日期格式错误；缺 `layout: post`；文件不在 `_posts/`。 |
| 样式错乱 | 修改了 theme 相关配置；`_config.yml` URL/baseurl 不一致。 |
| 中文标题乱码 | 文件保存为 UTF-8；编辑器不要用错误编码保存。 |

---

## 九、修订记录

- 2026-05-09：初稿，对应 Holmeslym.github.io Jekyll + Minima 默认结构。

若要扩展评论功能，可后续接入 **giscus**（基于 GitHub Discussions），需在模板中插入脚本并在仓库开启 Discussions——不属于必需项，可按需再加。
